@library('jenkins-shared-library') _

// create a var of maptype and set the value

def configMap = [
    type: "nodejsEKS"
    component: "backend"
    project: "expense"
]

pipelineDecision.decidePipeline(configMap)

if(! env.BRANCH_NAME.equalsIgnorecase('main')){
    pipelineDecision.decidePipeline(configMap)   
}
else{
    echo "Proceed with non-production or cr pipeline"
}