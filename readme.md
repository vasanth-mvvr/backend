## sonarQube
In the Jenkins Agent
* First install the plugin sonarQube
* Next, in the tools install the sonar version
* Next in the system configure the sonar bu give the name and credentials the credentials are generated from the sonarqure security tokens
* copy the credentials from the sonarqube paste it in by creating the credentials and add it.


# ** To add sonarqube webhook we need to go to configuration file **
* Name 
* url : <http://jenkins url>:9000/sonarqube-webhook/